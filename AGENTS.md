# AGENTS.md — xhs-test-project

本文件适用于所有 AI 编码助手（Claude、Codex、Cursor、Gemini CLI 等）。
定义了三个可调用的工作流，通过自然语言关键词触发。

---

## Profile 文件（自动维护，无需手动填写）

执行任何工作流前，先检查并读取以下文件：

| 文件 | 用途 |
|------|------|
| `profile/xhs-config.yaml` | 账号配置：签名句、视觉风格、选题方向、已用话题 |
| `profile/account.md` | 账号背景：定位、竞品结论、已发布内容、评判标准 |

**处理规则：**
- 文件已存在且字段有值 → 直接读取，不再询问
- 文件不存在或字段为空 → 通过对话向用户收集，**收集完后自动写入文件**，下次不再问
- 工作流执行过程中产生新信息（如确定了签名句、用了新话题）→ 工作流结束前自动更新对应字段

用户**不需要**手动编辑这两个文件，AI 负责维护。

**补充与修正：**

用户随时可以说"补充一下"、"我想改一下"、"再加一条"，AI 会：
1. 询问要补充/修改的具体内容
2. 将新信息**合并**写入 profile，不覆盖已有内容
3. 确认更新后的字段给用户看

触发关键词示例：
- "补充一下调研结论"
- "再加一个选题方向"
- "签名句我想换一个"
- "刚发现一个竞品，加进去"

---

## 触发方式

| 用户说 | 执行的工作流 |
|--------|------------|
| "帮我调研" / "调研竞品" / "/xhs-research" | → 执行 [XHS-RESEARCH] |
| "帮我写笔记" / "产出笔记素材" / "/xhs-note" | → 执行 [XHS-NOTE] |
| "做测试页面" / "制作测试" / "/new-test" | → 执行 [NEW-TEST] |

---

## [XHS-RESEARCH] 小红书账号调研

### 触发条件
用户提到"调研"、"竞品分析"、"分析账号"，或直接说 `/xhs-research`。

### Step 1：判断当前状态（必须执行，不得跳过）

检查项目目录下是否已有调研报告文件（`*-调研报告.md`、`调研分析总览.md` 等），或对话中是否已有调研结论。

- **没有任何调研结论**：进入 Step 2。
- **已有调研结论**：提炼不超过 10 条核心结论展示给用户，然后明确询问：
  - A. 直接使用已有结论继续
  - B. 重新调研（覆盖旧结论）
  - **等待用户回复，不得自行决定。**

### Step 2：收集素材

询问用户：
1. 账号方向是什么（一句话描述）
2. 素材在哪里（文件路径或直接粘贴）

读取所有可读的素材文件（CSV、Markdown、截图等）。

### Step 3：数据层分析

**先逐账号分析，再横向对比，不得直接合并。**

每个账号单独输出：
- 账号名 + ID + 内容类型
- 笔记总数、总阅读、平均阅读、评论率、分享率
- 表现最好的 2-3 篇（标题 + 阅读 + 说明为什么好）
- 表现最差的 2-3 篇（标题 + 阅读 + 说明为什么差）
- 这个账号的核心打法总结（1-2句）

逐账号分析完后，输出横向对比：
- 不同打法之间的数据差异（哪种打法平均阅读更高）
- 哪些账号定位相似但效果差异大，原因是什么
- 重点关注**评论率**（评论/阅读），这是测试类账号的核心健康指标

### Step 4：内容层分析

**同样先逐账号，再横向对比。**

每个账号单独分析：
- 标题句式：这个号用什么句式，哪篇效果好
- 封面设计：信息结构、用户行为逻辑
- 正文结构：钩子、共鸣、悬念、行动指令各占比
- 结果文案：给标签还是给共鸣，有无"许可机制"
- 评论区设计：有无引导格式、置顶氛围营造

逐账号分析完后，输出横向对比：
- 哪种封面逻辑点击率更高
- 哪种标题句式评论率更高
- 不同人设（工具号 vs 轻人设号）的数据差异

### Step 5：产出调研报告

报告结构如下，**每条结论必须有数据或截图依据，不泛泛而谈**：

**一、各账号速览**
数据总表，一行一个账号：账号名 / 笔记数 / 总阅读 / 平均阅读 / 评论率 / 分享率 / 内容类型

**二、分账号深度分析**
每个账号一节，包含：
- 账号定位与人设
- 内容打法（选题策略、标题句式、封面逻辑）
- 爆款拆解（具体笔记 + 数据 + 成功原因）
- 失败案例（具体笔记 + 数据 + 失败原因）

**三、打法归类与规律**
不按账号，按打法类型归类：
- 每种打法有哪几个号在用、数据表现如何
- 这种打法的核心机制是什么

**四、核心结论**
基于数据的可复用规律，每条结论注明来源账号和数据依据。

**五、必须避开的坑**
有多个竞品踩过的错误，直接列出，不写泛泛注意事项。

**六、【note 摘要】——传递给 /xhs-note 的结构化信息**

这一节是给下一个工作流直接使用的，格式固定，必须包含：

```
账号签名句候选：
  1. （候选一）— 逻辑说明
  2. （候选二）— 逻辑说明
  3. （候选三）— 逻辑说明

推荐封面逻辑：（封面即第一题 / 情绪共鸣型 / 金句型）
  理由：（一句话，附竞品数据依据）

标题句式优先级：
  1. （句式一）— 适用场景 + 竞品验证数据
  2. （句式二）— 适用场景 + 竞品验证数据
  3. （句式三）— 适用场景 + 竞品验证数据

选题方向池（按优先级）：
  1. 方向名称 — 数据依据
     切入角度：
       - 角度A（具体笔记选题，可直接用）
       - 角度B
       - 角度C
       - 角度D
       - 角度E
  2. 方向名称 — 数据依据
     切入角度：
       - 角度A
       - 角度B
       - 角度C
       - 角度D
       - 角度E

正文写法参考：
  - 钩子：（竞品最有效的钩子写法，附例子）
  - 共鸣段：（写法规律）
  - 结果文案：（许可机制写法，附最佳竞品例子）
  - 评论区引导：（最有效的格式，附例子）

必须避开：
  - （坑一）
  - （坑二）
```

以 Markdown 文件保存到项目目录，文件名 `{账号方向}-调研报告.md`。
在对话中给出不超过 10 条核心结论摘要。

### Step 6：询问是否需要增补调研

报告产出后，必须明确问用户：

> "调研报告已完成。你是否有想补充调研的内容？比如：
> - 新增竞品账号数据
> - 补充某类话题的案例
> - 对某个结论有疑问想深入分析
>
> 有的话告诉我，没有的话我们可以直接进入下一步（出笔记素材）。"

**等待用户回复，按以下分支处理：**

- **有补充内容** → 询问具体补充什么（新账号数据/新截图/新文案），读取后将新发现**合并**追加到已有报告末尾，更新 profile，然后再次询问是否还有补充
- **没有补充** → 将报告核心结论同步写入 `profile/account.md`，提示用户可以运行 `/xhs-note` 开始出笔记素材

---

## [XHS-NOTE] 产出笔记素材

### 触发条件
用户提到"写笔记"、"笔记素材"、"帮我写一篇"，或直接说 `/xhs-note`。

### 前提检查
需要有调研报告文件（`*-调研报告.md`）。没有则提示先执行 [XHS-RESEARCH]。

读取调研报告中的**【note 摘要】**章节作为主要输入，完整报告作为参考背景。
同时读取 `profile/xhs-config.yaml` 和 `profile/account.md`。

### Step 1：展示账号背景并确认

从【note 摘要】读取以下信息，**展示给用户确认，不直接使用**：

```
签名句：（从报告摘要读取）
封面逻辑：（从报告摘要读取）
标题句式优先级：（从报告摘要读取）
```

明确问用户：
> "以上是根据调研报告得出的账号设定，你是否需要调整？
> 可以直接告诉我改哪一项，或者说'没问题'直接继续。"

**等待用户确认，按分支处理：**

- **没问题** → 直接进入 Step 2

- **需要调整** →
  1. 拉取完整调研报告中与该设定相关的章节（如要改封面逻辑，读"打法归类与规律"和对应竞品的封面分析）
  2. 基于完整报告数据给出判断，格式：
     > "调研数据显示：（竞品X）用（用户想改成的方案），平均阅读（数据）；（竞品Y）用（推荐方案），平均阅读（数据）。建议（保留/调整）的原因是（一句话）。你确定要改吗？"
  3. 用户坚持修改 → 接受修改，**同步更新 profile**
  4. 用户接受建议 → 保留原设定，继续 Step 2

### Step 2：推荐选题角度并确认

从【note 摘要】的选题方向池中，按优先级找到当前方向下**未用过的切入角度**推荐给用户：

> "根据调研报告，建议这篇做「（具体角度）」，属于「（方向名）」方向，原因是（一句话数据依据）。
> 你也可以选其他角度：
> 2. （角度B）
> 3. （角度C）
> 或者告诉我你想做的方向。"

**角度耗尽时的处理：**
- 某个方向下所有角度都用过了 → AI 基于该方向的爆款规律和竞品数据，自动推演 3-5 个新角度，展示给用户确认后追加到 profile
- 所有方向的角度都用完了 → AI 基于调研报告的核心规律（什么话题+什么形式=高评论率），推演 2-3 个全新方向，展示给用户确认后追加到 profile 的方向池

**等待用户确认，不得自行决定。**

### Step 3：产出笔记素材

**笔记标题**：给出 3 个版本，覆盖不同句式：
- 「为什么你总X」— 诊断命名型，点击动机最强
- 「方法+结果型」— 算法友好，搜索流量入口
- 「悬念结论型」— 反差感，适合A/B测试

每个版本说明适用场景。

**封面文字**：按确认后的封面逻辑输出。
- 若是"封面即第一题"逻辑：给出第一道题 + A/B/C/D 选项完整文字
- 顶部必须有账号签名句

**正文**：
结构：钩子（前2行）→ 共鸣段（3-5行）→ 悬念铺垫（2-3行）→ 行动指令（1-2行）
语气与账号调性一致，不套模板。参考【note 摘要】中的钩子和共鸣段写法。

**评论区引导语**：结尾互动指令，给用户具体的报结果格式。

**置顶评论模板**：账号自己先发的那条，制造社区氛围。

### Step 4：询问用户是否满意

素材产出后，明确问用户：

> "以上是本篇笔记素材，你觉得怎么样？
> - 满意 → 我保存文件并更新账号记录
> - 需要调整 → 告诉我改哪部分（标题/封面/正文/评论区），我重新来过"

**按分支处理：**
- **满意** → 进入 Step 5
- **需要调整** → 询问具体修改哪部分，重新产出对应内容，再次询问是否满意，直到用户确认

### Step 5：保存并更新 profile

用户确认满意后：
1. 保存笔记素材到 `notes/{选题}-笔记.md`
2. 将本篇用过的**具体角度**写入 `profile/xhs-config.yaml` 对应方向的 `angles_used`
3. 如果 Step 1 中用户修改了签名句/封面逻辑/标题句式，同步更新 profile 对应字段
4. 检查当前方向剩余角度数量，若少于 2 个，提示用户："「（方向名）」方向的角度快用完了，我已准备好新角度，下次出笔记时会自动推荐"

### Step 6：判断是否需要测试页面

如果笔记核心是"答题得结果"，询问用户是否需要制作测试页面。需要则执行 [NEW-TEST]。

### Step 7：询问是否继续出下一篇

测试页面处理完后（或用户不需要测试页面），询问：

> "这篇完成了。要继续出下一篇吗？"

**按分支处理：**
- **继续** → 直接回到 Step 2（跳过 Step 1，签名句/封面逻辑/标题句式已确认，不重复问）。从选题方向池中推荐下一个未用过的角度
- **不继续** → 结束，提示当前已用角度列表，告知下次直接说"帮我写笔记"可继续产出

**注意：同一份调研报告可以无限次产出笔记，不需要重新调研。调研报告只在以下情况需要更新：**
- 用户主动说"我要重新调研"
- 用户提供了新的竞品数据

### 输出格式

在对话中直接输出所有素材，同时保存到 `notes/{选题}-笔记.md`。

---

## [NEW-TEST] 制作测试页面

### 触发条件
用户提到"做测试页面"、"测试题"、"部署测试"，或直接说 `/new-test`。

### 前提检查
需要有选题名称和结果类型列表（由 [XHS-NOTE] 产出，或用户直接提供）。

### Step 1：收集必要信息

从上下文或询问用户获取：
1. 选题名称（用于文件夹命名和页面标题）
2. 结果类型列表（如：大理、成都、厦门……）
3. 账号调性（颜色风格、语气）

### Step 2：设计题目

根据选题和结果类型，设计 6-10 道题目：
- 每道题 3-4 个选项
- 每个选项对应评分（scores 对象，key 为结果类型）
- 题目要有"灵魂拷问感"，不要有"填表感"
- 选项之间情绪差异要明显

**验证规则（必须通过）**：确认每个结果类型在"最优选择路径"下都能成为最高分。不通过则调整评分直到全部通过。

### Step 3：写结果文案

每个结果类型包含：
- `type`：类型名称（唯一）
- `subtitle`：类型标签 + 一句定性描述
- `traits`：4个关键词
- `analysis`：2-3句共情分析，写"你是这样的人"
- `advice`：1-2句许可机制收尾，公式"原来你X是因为Y——这是你的特质，不是缺点"

### Step 4：制作页面

在 `tests/{选题名称}/` 目录下创建：
- `config.json`：题目和结果数据
- `index.html`：测试页面

页面功能要求：
- 开始页、答题页、结果页三个状态
- 上一题/下一题导航
- 选中态视觉反馈
- 正常模式下选完自动跳下一题
- 最后一题变为"查看结果"按钮
- 结果页展示图片 + 分享引导

颜色风格从调研报告的封面视觉方案获取。

### Step 5：验证并部署

1. 启动本地服务器验证（`npx serve tests/{选题名称}`）
2. 确认每个结果页面正常显示
3. commit 并 push 到远端

**部署平台按优先级自动判断：**
- 有 `.github/workflows/` → GitHub Pages
- 有 `vercel.json` → Vercel
- 有 `netlify.toml` → Netlify
- 以上都没有 → 只推代码，告知用户尚未配置部署平台，列出三个平台接入方式供选择

4. 返回线上访问地址

### 输出

- `tests/{选题名称}/config.json`
- `tests/{选题名称}/index.html`
- `tests/{选题名称}/images/` 目录
- 线上访问地址（如已配置部署）
