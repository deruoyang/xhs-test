# AGENTS.md — xhs-test-project

本文件适用于所有 AI 编码助手（Claude、Codex、Cursor、Gemini CLI 等）。
定义了三个可调用的工作流，通过自然语言关键词触发。

---

## Profile 文件（每次工作流开始前自动读取）

执行任何工作流前，先检查并读取以下文件：

| 文件 | 用途 |
|------|------|
| `profile/xhs-config.yaml` | 账号配置：签名句、视觉风格、选题方向、已用话题 |
| `profile/account.md` | 账号背景：定位、竞品结论、已发布内容、评判标准 |

- 文件存在 → 直接读取，**不再询问用户已有答案的问题**
- 文件不存在 → 运行结束后提示用户创建，并提供模板

---

## 触发方式

| 用户说 | 执行的工作流 |
|--------|------------|
| "帮我调研" / "调研竞品" / "/xhs-research" | → 执行 [XHS-RESEARCH] |
| "帮我写笔记" / "产出笔记素材" / "/xhs-note" | → 执行 [XHS-NOTE] |
| "做测试页面" / "制作测试" / "/new-test" | → 执行 [NEW-TEST] |

---

## [XHS-RESEARCH] 小红书账号调研

### 触发条件
用户提到"调研"、"竞品分析"、"分析账号"，或直接说 `/xhs-research`。

### Step 1：判断当前状态（必须执行，不得跳过）

检查项目目录下是否已有调研报告文件（`*-调研报告.md`、`调研分析总览.md` 等），或对话中是否已有调研结论。

- **没有任何调研结论**：进入 Step 2。
- **已有调研结论**：提炼不超过 10 条核心结论展示给用户，然后明确询问：
  - A. 直接使用已有结论继续
  - B. 重新调研（覆盖旧结论）
  - **等待用户回复，不得自行决定。**

### Step 2：收集素材

询问用户：
1. 账号方向是什么（一句话描述）
2. 素材在哪里（文件路径或直接粘贴）

读取所有可读的素材文件（CSV、Markdown、截图等）。

### Step 3：数据层分析

从数据文件提炼：
- 哪些笔记阅读/评论/分享最高
- 不同话题的表现差异
- 爆款与低效内容的分界线
- 重点关注**评论率**（评论/阅读），这是测试类账号的核心健康指标

### Step 4：内容层分析

分析：
- 标题句式：哪种出现频率高、表现好
- 封面设计：信息结构、用户行为逻辑
- 正文结构：钩子、共鸣、悬念、行动指令
- 评论区设计：有无引导格式、置顶氛围营造

### Step 5：产出调研报告

报告必须包含（每条结论须有数据依据，不泛泛而谈）：

1. **数据层核心发现**：爆款规律、低效内容共同点、评论率/分享率问题
2. **内容层核心发现**：有效标题句式（附真实例子）、封面逻辑、正文规律
3. **账号定位建议**：人设方向、账号签名句候选（3-5个）、选题方向池（按优先级）、封面视觉方案
4. **必须避免的坑**：竞品踩过的、有数据支撑的错误

以 Markdown 文件保存到项目目录，文件名 `{账号方向}-调研报告.md`，同时在对话中给出不超过 10 条核心结论摘要。

---

## [XHS-NOTE] 产出笔记素材

### 触发条件
用户提到"写笔记"、"笔记素材"、"帮我写一篇"，或直接说 `/xhs-note`。

### 前提检查
需要有调研报告（文件或对话中已有结论均可）。没有则提示先执行 [XHS-RESEARCH]。

### Step 1：确认账号背景

从调研报告或对话上下文获取：
- 账号人设方向
- 账号签名句
- 封面视觉方案
- 有效标题句式

信息不完整时直接问用户，不要猜。

### Step 2：确定选题

询问用户本篇笔记的选题方向，结合调研报告选题优先级给出建议，最终由用户决定。

### Step 3：产出笔记素材

**笔记标题**：给出 3 个版本，覆盖不同句式：
- 「为什么你总X」— 诊断命名型，点击动机最强
- 「方法+结果型」— 算法友好，搜索流量入口
- 「悬念结论型」— 反差感，适合A/B测试

每个版本说明适用场景。

**封面文字**：按调研报告确定的封面逻辑输出。
- 若是"封面即第一题"逻辑：给出第一道题 + A/B/C/D 选项完整文字
- 顶部必须有账号签名句

**正文**：
结构：钩子（前2行）→ 共鸣段（3-5行）→ 悬念铺垫（2-3行）→ 行动指令（1-2行）
语气与账号调性一致，不套模板。

**评论区引导语**：结尾互动指令，给用户具体的报结果格式。

**置顶评论模板**：账号自己先发的那条，制造社区氛围。

### Step 4：判断是否需要测试页面

如果笔记核心是"答题得结果"，询问用户是否需要制作测试页面。需要则执行 [NEW-TEST]。

### 输出格式

在对话中直接输出所有素材，同时保存到 `notes/{选题}-笔记.md`。

---

## [NEW-TEST] 制作测试页面

### 触发条件
用户提到"做测试页面"、"测试题"、"部署测试"，或直接说 `/new-test`。

### 前提检查
需要有选题名称和结果类型列表（由 [XHS-NOTE] 产出，或用户直接提供）。

### Step 1：收集必要信息

从上下文或询问用户获取：
1. 选题名称（用于文件夹命名和页面标题）
2. 结果类型列表（如：大理、成都、厦门……）
3. 账号调性（颜色风格、语气）

### Step 2：设计题目

根据选题和结果类型，设计 6-10 道题目：
- 每道题 3-4 个选项
- 每个选项对应评分（scores 对象，key 为结果类型）
- 题目要有"灵魂拷问感"，不要有"填表感"
- 选项之间情绪差异要明显

**验证规则（必须通过）**：确认每个结果类型在"最优选择路径"下都能成为最高分。不通过则调整评分直到全部通过。

### Step 3：写结果文案

每个结果类型包含：
- `type`：类型名称（唯一）
- `subtitle`：类型标签 + 一句定性描述
- `traits`：4个关键词
- `analysis`：2-3句共情分析，写"你是这样的人"
- `advice`：1-2句许可机制收尾，公式"原来你X是因为Y——这是你的特质，不是缺点"

### Step 4：制作页面

在 `tests/{选题名称}/` 目录下创建：
- `config.json`：题目和结果数据
- `index.html`：测试页面

页面功能要求：
- 开始页、答题页、结果页三个状态
- 上一题/下一题导航
- 选中态视觉反馈
- 正常模式下选完自动跳下一题
- 最后一题变为"查看结果"按钮
- 结果页展示图片 + 分享引导

颜色风格从调研报告的封面视觉方案获取。

### Step 5：验证并部署

1. 启动本地服务器验证（`npx serve tests/{选题名称}`）
2. 确认每个结果页面正常显示
3. commit 并 push 到远端

**部署平台按优先级自动判断：**
- 有 `.github/workflows/` → GitHub Pages
- 有 `vercel.json` → Vercel
- 有 `netlify.toml` → Netlify
- 以上都没有 → 只推代码，告知用户尚未配置部署平台，列出三个平台接入方式供选择

4. 返回线上访问地址

### 输出

- `tests/{选题名称}/config.json`
- `tests/{选题名称}/index.html`
- `tests/{选题名称}/images/` 目录
- 线上访问地址（如已配置部署）
